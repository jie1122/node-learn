{
  "configurations": [

    {
      "type": "node",
      "request": "launch",
      "name": "Launch Current File",
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Current ts File",
      "runtimeArgs": ["--loader", "ts-node/esm"],
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
  
}
